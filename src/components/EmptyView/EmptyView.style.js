import { WHITE } from 'constants/colours';

export default {
  container: {
    flex: 1,
    backgroundColor: WHITE,
  },

  wrapper: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
};
